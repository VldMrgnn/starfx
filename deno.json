{
  "tasks": {
    "types": "deno run --allow-write ./api-type-template.ts",
    "npm": "deno run --allow-write ./npm.ts"
  },
  "lint": {
    "exclude": ["npm/", "examples/"],
    "rules": {
      "tags": ["recommended"],
      "exclude": ["no-explicit-any", "require-yield"]
    }
  },
  "fmt": {
    "exclude": ["npm/", "examples/"]
  }
}
